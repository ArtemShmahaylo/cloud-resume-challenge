---
- name: Deploy CloudFormation stack
  hosts: localhost
  connection: local
  gather_facts: false

  vars:
    template_path: "{{ (playbook_dir, '..', 'frontend.yaml') | path_join | realpath }}"

    parameters_file: "{{ (playbook_dir, '..', 'vars', 'parameters.yml') | path_join | realpath }}"

    cfn_parameters:
      BucketName: 'www.artemshmahaylo.com'
      ACMCertificateArn: 'arn:aws:acm:us-east-1:471112966659:certificate/aa81721a-cce3-495d-8933-da92cf5ed905'

  vars_files:
    - "{{ playbook_dir }}/vaults/prod.yml"

  tasks:

    - name: Deploy / Update CloudFormation stack
      amazon.aws.cloudformation:

        aws_access_key: "{{ AWS_ACCESS_KEY_ID }}"
        aws_secret_key: "{{ AWS_SECRET_ACCESS_KEY }}"
        region: "{{ AWS_REGION }}"

        stack_name: "{{ STACK_NAME }}"
        state: present

        template: "{{ template_path }}"
        template_parameters: "{{ cfn_parameters }}"

        capabilities:
          - CAPABILITY_NAMED_IAM
          - CAPABILITY_AUTO_EXPAND

        on_create_failure: DO_NOTHING
        create_timeout: 30

      register: cfn_result

    - name: Show CloudFormation outputs
      debug:
        var: cfn_result.stack_outputs
